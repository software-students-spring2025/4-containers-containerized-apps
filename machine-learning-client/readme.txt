This component handles the object detection functionality for the containerized application. It captures video frames from a camera, processes them to identify objects, and stores the results in a MongoDB database.

Features

- Real-time object detection using computer vision
- Support for multiple detection models (SSD MobileNet, YOLO)
- MongoDB integration for storing detection results
- Configurable detection parameters and intervals
- Containerized deployment with Docker

